//This file will store the search history for a particular session. The Search screen will append new searches
//to this array as the user inputs them. All searches will dissapear when app shuts down.
//Each element contains a single property, called 'term'. More properties can be added to each element as needed.

searches = [{term: "Search History:"}];